#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v{1,2,3,4,5};
    //for array
    //binary_search(arr,arr 
    
    
    + size,3)
    if(binary_search(v.begin() , v.end(), 3)){
       cout<<"found"<<endl;
    }
    else{
        cout<<"not found"<<endl;
    }
    
    
    
}